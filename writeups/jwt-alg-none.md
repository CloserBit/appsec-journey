# JWT alg:none — обход проверки подписи (OWASP Juice Shop)

## Summary
Приложение не проверяет подпись, если в header указан alg:"NONE". Благодаря этому подделывается payload и есть возможность сменить пользователя на любого существующего и обойти авторизацию.

## CWE
CWE-347 — Improper Verification of Cryptographic Signature
(+ можно CWE-345 — недостаточная проверка подлинности данных)

## Severity
Оценка: Critical - рушится вся система идентификации пользователей. Приложение не может больше доверять ни одному токену, так как любой из них может быть подделан.

## Root cause
Почему это вообще возможно: сервер доверяет полю `alg` из
недоверенного токена; проверка через blocklist (`== "none"`),
пропускающий `NONE`. Payload — открытый Base64, легко подделать.

## Steps to Reproduce (PoC)
1. Логин, получить токен (alg: RS256).
2. jwt_tool ... / ручная сборка: header {"alg":"NONE"}, payload с role:admin, пустая подпись.
3. Удалить If-None-Match, подставить Authorization: Bearer <токен>.
4. GET /rest/admin/application-configuration → 200 + админский конфиг.
(вставь ключевые куски запроса/ответа — без полного токена)

## Impact
Твой ответ на «третий вопрос» — разверни в 2-3 предложения.

## Fix
Allowlist: сервер принимает ТОЛЬКО RS256, игнорирует alg из токена.
Ссылка на principles.md → allowlist > blocklist.

## Detection (как поймать в пайплайне)
DAST/ручной тест (jwt_tool -X a). SAST — если видит код проверки токена.